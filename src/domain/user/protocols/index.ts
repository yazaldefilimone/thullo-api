export type userStore = {
  id: string;
  name: string;
  email: string;
  created_at: string;
};
export type user = {
  id: string;
  name: string;
  email: string;
  createdAt: Date;
};

export type userCreate = {
  name: string;
  email: string;
};
